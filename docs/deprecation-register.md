# 兼容协议登记

| 协议 | 状态 | 消费者与退出条件 |
| --- | --- | --- |
| 帖子列表 coverImages | 保留兼容，未安排删除 | Web / Mobile 旧版本仍消费；只有无消费者、数据迁移完成、回归覆盖、回滚路径明确后才可独立 PR 删除 |
| 缺失 coverMedia 的旧响应 | 消费者须支持 | 后端先发布，Web / Mobile 支持缺字段静态占位；不按后缀猜测动画，不自动加载未知原图 |
| 历史 GIF 缺 poster_url | 保留未知状态，不回填 | 新列表显示占位、详情仍可查看；补生成必须经独立可审计任务验证，不能伪造派生 URL |
| Markdown v3/v4/v5 与历史段内 LF | 保留兼容，不迁移 | 块边界 v1 仅修复对齐标记消费；Web/Windows 同步共享语料并完成真实阅读、编辑、复制、保存重开验收后交付。空格与 WJ 原样保存；无消费者、迁移、回归、回滚四项证据前不得删除兼容 |

本轮增加兼容接口、新上传静态首帧及可选双档动画 WebP 列表预览；原 GIF 与详情 URL 保留，不清理原图、不修复或回填历史媒体。部署按后端先行、消费者随后进行。

- 5.20 保留 previewVariants 缺字段/null 的旧服务兼容，可信动画可在选中时回退原 GIF；不删除 coverImages、原 url 或历史对象。该兼容退出仍须独立四项证据评审。

- 5.20.1 修复主题/动态夹及同名动态兼容计数为当前可见总数；保留旧 `momentBookmarkCount` 字段和旧目录 ID 映射，不清理历史收藏。Web / Mobile / Foundation 需同步计数说明和状态变化回归，字段清理仍须独立评审。

- 动态帖列表静止策略复用现有 `MomentMediaResponseDto`，无新增或删除字段。GIF 原件 `url` 保留给详情播放和保存；缺少静态缩略图的历史动态在列表保持占位，不回填、不猜地址。动态表情 `mediumUrl=url` 兼容字段继续保留，静止预览使用 `thumbnailUrl`。本条不改变主题帖的预览兼容政策。

## 富文本兼容保留登记

本页登记 Backend 富文本专项的兼容边界，治理级清理要求仍见[统一弃用规则](https://github.com/morenk/wenyousite-workspace/blob/main/AGENTS.md#清理门禁)。新增行为 fixture/schema 仅用于测试，不表示移除任何正文协议或已发布客户端。

| 保留项 | 处理边界 | 删除/收紧前必须具备的证据 |
| --- | --- | --- |
| Markdown v3/v4/v5 已支持正文 | 保留按实际能力读取；v5 编号不能证明引用空行或无损保护已接入 | 无消费者、数据已迁移、回归覆盖、回滚路径明确；独立 PR |
| 历史 LF、双 LF 与引用独占 `>` | 保留原段内/段落归属，不猜测按键来源，不批量改写 | 同上，不能以新编辑器输入规则覆盖历史语义 |
| clipboard v1 与 v2 | 合法 v1 恢复结构但不恢复对齐；合法 v2 严格重建；无有效载荷保持可见文本回退 | 接收能力和跨应用回退均有消费证据，不因平台名称跳过合法 envelope |
| 未知/不可无损转换原文 | 安全阅读与原文保留；不把降级结果当作可覆盖来源；只读且无新编辑时可保留原文快照后退出 | 明确读取、编辑、保存保护能力及失败回归；不能为通过 fixture 删除未知内容 |
| newline v1 revision 2、roundtrip v7 | 原 ID 和语义不变，新增测试契约继续引用 | 客户端已同步并验证对应独立语料后才能淘汰测试版本；不同时清理正文兼容 |

[能力与停止/回退验证](rich-text-stability-delivery.md)只提供离线证据和执行方案，不授权部署、停止线上写入口、回退进程或发布 APK；这些动作继续由负责人明确批准。

- 5.21 主贴聚合保存新增可选 `defaultSubthreadPostingPolicy`；Web / Mobile 旧客户端省略字段保留原策略。保留现有子贴权限更新端点、三个枚举与创建默认值，本次不弃用或清理协议。后端先行、消费者随后；将来收紧字段或删除旧端点仍需独立四项证据评审。

## 完整动画展示资源兼容演进

新增统一 `display` / `avatarDisplay` 与正文 `mediaDisplays`；来源身份和旧字段保留，历史补处理及删除原件尚未执行。详细字段、跨场景选择和发布边界见[完整动画 WebP 展示契约](media-display.md)。

- 5.23 自定义收藏夹重命名/删除是向后兼容的纯新增接口，无弃用清理；保留旧目录 ID 映射、`momentBookmarkCount` 兼容字段和所有快捷收藏协议。Foundation 只做影响审查，无视觉契约改动；Web/Windows Flutter 同步固定 OpenAPI 并覆盖管理操作。目录删除是用户请求的业务操作，不属于兼容协议清理。
